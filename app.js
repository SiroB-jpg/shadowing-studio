"use strict";
const $=id=>document.getElementById(id);
const DB="ISS_V08", SS="sentences", AS="audioCache";
const App={db:null,sentences:[],analysed:[],alice:null,currentAudio:null,currentAudioResolve:null,playbackContext:"main",cur:{book:"",chapter:"",group:1,index:0},verbTenseIndex:0};

const Util={esc:s=>String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m])),clean:s=>String(s??"").replace(/^["']|["']$/g,"").trim(),uniq:a=>[...new Set(a)],nat:(a,b)=>String(a).localeCompare(String(b),undefined,{numeric:true,sensitivity:"base"}),sleep:ms=>new Promise(r=>setTimeout(r,ms)),gnum:s=>Math.floor((Number(s.order)-1)/10)+1,sortS:(a,b)=>String(a.book).localeCompare(String(b.book))||String(a.chapter).localeCompare(String(b.chapter),undefined,{numeric:true,sensitivity:"base"})||Number(a.order)-Number(b.order)};

const Storage={open(){return new Promise((res,rej)=>{let r=indexedDB.open(DB,1);r.onupgradeneeded=e=>{let d=e.target.result;if(!d.objectStoreNames.contains(SS))d.createObjectStore(SS,{keyPath:"id",autoIncrement:true});if(!d.objectStoreNames.contains(AS))d.createObjectStore(AS,{keyPath:"key"});};r.onsuccess=e=>res(e.target.result);r.onerror=e=>rej(e.target.error);});},store(n,m="readonly"){return App.db.transaction(n,m).objectStore(n);},all(n){return new Promise((res,rej)=>{let r=this.store(n).getAll();r.onsuccess=()=>res(r.result);r.onerror=()=>rej(r.error);});},get(n,k){return new Promise((res,rej)=>{let r=this.store(n).get(k);r.onsuccess=()=>res(r.result);r.onerror=()=>rej(r.error);});},put(n,o){return new Promise((res,rej)=>{let t=App.db.transaction(n,"readwrite");t.objectStore(n).put(o);t.oncomplete=res;t.onerror=()=>rej(t.error);});},addMany(items){return new Promise((res,rej)=>{let t=App.db.transaction(SS,"readwrite"),s=t.objectStore(SS);items.forEach(x=>s.add(x));t.oncomplete=res;t.onerror=()=>rej(t.error);});},clear(n){return new Promise((res,rej)=>{let r=this.store(n,"readwrite").clear();r.onsuccess=res;r.onerror=()=>rej(r.error);});}};

const Library={parseCSV(text,defs){text=String(text||"").replace(/^\uFEFF/,"");let rows=[],row=[],f="",q=false;for(let i=0;i<text.length;i++){let c=text[i],n=text[i+1];if(c=='"'&&q&&n=='"'){f+='"';i++;}else if(c=='"')q=!q;else if(c==","&&!q){row.push(f);f="";}else if((c=="\n"||c=="\r")&&!q){if(c=="\r"&&n=="\n")i++;row.push(f);f="";if(row.some(x=>x.trim()))rows.push(row);row=[];}else f+=c;}row.push(f);if(row.some(x=>x.trim()))rows.push(row);if(!rows.length)return[];let heads=rows[0].map(x=>x.trim().toLowerCase()),has=heads.includes("italian")||heads.includes("sentence")||heads.includes("english"),data=has?rows.slice(1):rows,idx=names=>{for(let n of names){let i=heads.indexOf(n);if(i>=0)return i;}return -1;},bi=idx(["book"]),ci=idx(["chapter","lesson","unit"]),oi=idx(["order","number","no","#"]),ii=idx(["italian","sentence","text","it","italiano"]),ei=idx(["english","translation","meaning","en"]);return data.map((r,i)=>{let italian="",english="";if(has){italian=ii>=0?r[ii]:"";english=ei>=0?r[ei]:"";}else if(r.length>=5){italian=r[3];english=r[4];}else if(r.length>=2){italian=r[0];english=r[1];}else italian=r[0];return{book:Util.clean(has&&bi>=0?r[bi]:defs.book),chapter:Util.clean(has&&ci>=0?r[ci]:defs.chapter),order:Number(Util.clean(has&&oi>=0?r[oi]:i+1))||i+1,italian:Util.clean(italian),english:Util.clean(english),bookmarked:false,difficult:false,notes:""};}).filter(x=>x.italian);},chapter(){return App.sentences.filter(s=>s.book==App.cur.book&&s.chapter==App.cur.chapter).sort(Util.sortS);},group(){return this.chapter().filter(s=>Util.gnum(s)==Number(App.cur.group));},current(){let g=this.group();if(!g.length)return null;App.cur.index=Math.max(0,Math.min(App.cur.index,g.length-1));return g[App.cur.index];},async refresh(){App.sentences=(await Storage.all(SS)).sort(Util.sortS);if(App.sentences.length&&!App.cur.book){let s=App.sentences[0];App.cur={book:s.book,chapter:s.chapter,group:Util.gnum(s),index:0};}UI.renderAll();}};

const UI={fill(sel,vals,val,label=x=>x){sel.innerHTML="";if(!vals.length){sel.innerHTML="<option>—</option>";return;}vals.forEach(v=>{let o=document.createElement("option");o.value=v;o.textContent=label(v);if(String(v)==String(val))o.selected=true;sel.appendChild(o);});},renderAll(){this.renderSelectors();this.renderTree();this.renderViewer();Verb.render();this.stats();},renderSelectors(){let books=Util.uniq(App.sentences.map(s=>s.book)).sort(Util.nat);if(!books.includes(App.cur.book)&&books[0])App.cur.book=books[0];let ch=Util.uniq(App.sentences.filter(s=>s.book==App.cur.book).map(s=>s.chapter)).sort(Util.nat);if(!ch.includes(App.cur.chapter)&&ch[0])App.cur.chapter=ch[0];let gs=Util.uniq(App.sentences.filter(s=>s.book==App.cur.book&&s.chapter==App.cur.chapter).map(Util.gnum)).sort((a,b)=>a-b);if(!gs.includes(Number(App.cur.group))&&gs[0])App.cur.group=gs[0];this.fill($("bookSel"),books,App.cur.book);this.fill($("chapterSel"),ch,App.cur.chapter);this.fill($("groupSel"),gs.map(String),String(App.cur.group),g=>"Group "+g);},renderTree(){let t=$("tree");t.innerHTML="";if(!App.sentences.length){t.innerHTML='<p class="small">No sentences imported yet.</p>';return;}Util.uniq(App.sentences.map(s=>s.book)).sort(Util.nat).forEach(b=>{let bd=document.createElement("div");bd.className="book "+(App.cur.book==b?"active":"");bd.textContent=b;bd.onclick=()=>{App.cur.book=b;App.cur.chapter="";App.cur.group=1;App.cur.index=0;UI.renderAll();};t.appendChild(bd);Util.uniq(App.sentences.filter(s=>s.book==b).map(s=>s.chapter)).sort(Util.nat).forEach(c=>{let cd=document.createElement("div");cd.className="chapter "+(App.cur.book==b&&App.cur.chapter==c?"active":"");cd.textContent=c;cd.onclick=()=>{App.cur.book=b;App.cur.chapter=c;App.cur.group=1;App.cur.index=0;UI.renderAll();};t.appendChild(cd);if(App.cur.book==b&&App.cur.chapter==c)Util.uniq(App.sentences.filter(s=>s.book==b&&s.chapter==c).map(Util.gnum)).sort((a,b)=>a-b).forEach(g=>{let gd=document.createElement("div");gd.className="groupItem "+(Number(App.cur.group)==g?"active":"");gd.textContent="Group "+g;gd.onclick=()=>{App.cur.group=g;App.cur.index=0;UI.renderAll();};t.appendChild(gd);});});});},card(s,active,i){let show=$("showEnglish").value=="show",d=document.createElement("div");d.className="card "+(active?"active":"");const selectCard=()=>{if(i!==undefined)SentenceController.jumpToIndex(i);};d.onclick=selectCard;d.addEventListener("touchend",e=>{if(e.target.closest("button"))return;e.preventDefault();selectCard();},{passive:false});d.innerHTML=`<span class="pill">${s.order}</span>${s.bookmarked?'<span class="pill">★ bookmarked</span>':""}<div class="italian">${Util.esc(s.italian)}</div>${show&&s.english?`<div class="english">${Util.esc(s.english)}</div>`:""}<div class="cardTools"><button class="mini light" data-a="play">Play</button><button class="mini light" data-a="bm">★ Bookmark</button><button class="mini light" data-a="edit">Edit</button></div>`;d.querySelector('[data-a="play"]').onclick=async e=>{e.stopPropagation();await Speech.speak(s.italian);};d.querySelector('[data-a="bm"]').onclick=async e=>{e.stopPropagation();s.bookmarked=!s.bookmarked;await Storage.put(SS,s);await Library.refresh();};d.querySelector('[data-a="edit"]').onclick=e=>{e.stopPropagation();Editor.open(s);};return d;},renderViewer(){let v=$("viewer");v.innerHTML="";if(!App.sentences.length){v.innerHTML='<div class="card"><h3>No sentences yet</h3><p>Import a CSV to begin.</p></div>';return;}v.innerHTML=`<h2>${Util.esc(App.cur.book)} — ${Util.esc(App.cur.chapter)} — Group ${App.cur.group}</h2>`;let g=Library.group(),q=$("search").value.trim().toLowerCase();if($("displayMode").value=="single"){let s=Library.current();if(s)v.appendChild(this.card(s,true));return;}g.filter(s=>!q||(s.italian+" "+s.english).toLowerCase().includes(q)).forEach((s,i)=>v.appendChild(this.card(s,i==App.cur.index,i)));},stats(){let books=Util.uniq(App.sentences.map(s=>s.book)).length,ch=Util.uniq(App.sentences.map(s=>s.book+"|"+s.chapter)).length,gs=Util.uniq(App.sentences.map(s=>s.book+"|"+s.chapter+"|"+Util.gnum(s))).length;$("stats").innerHTML=`${App.sentences.length} sentences<br>${books} book(s), ${ch} chapter(s), ${gs} group(s)`;},status(msg,cls=""){$("status").textContent=msg;$("status").className="status "+cls;}};

const PlaybackControls={rate(){let id=App.playbackContext==="verb"&&$("verbRate")?"verbRate":"rate";return Number($(id).value)||1;},pause(){let id=App.playbackContext==="verb"&&$("verbPause")?"verbPause":"pause";return Number($(id).value)||0;},repeat(){let id=App.playbackContext==="verb"&&$("verbRepeat")?"verbRepeat":"repeat";let v=$(id).value;return v==="infinite"?"infinite":Number(v)||1;}};

const Speech={loadVoices(){let vs=speechSynthesis.getVoices();App.alice=vs.find(v=>v.name=="Alice")||vs.find(v=>/alice/i.test(v.name))||vs.find(v=>v.lang&&v.lang.toLowerCase().startsWith("it"))||null;},stop(){speechSynthesis.cancel();if(App.currentAudio){try{App.currentAudio.pause();App.currentAudio.src="";App.currentAudio.load();}catch(e){}App.currentAudio=null;}if(App.currentAudioResolve){try{App.currentAudioResolve();}catch(e){}App.currentAudioResolve=null;}},system(text){return new Promise(res=>{speechSynthesis.cancel();let u=new SpeechSynthesisUtterance(text);u.lang="it-IT";u.rate=PlaybackControls.rate();if(App.alice)u.voice=App.alice;u.onend=res;u.onerror=res;speechSynthesis.speak(u);});},playBlob(blob){return new Promise((res,rej)=>{this.stop();let url=URL.createObjectURL(blob),a=new Audio(url),timer=null,settled=false;App.currentAudio=a;const cleanup=()=>{if(timer)clearTimeout(timer);try{URL.revokeObjectURL(url);}catch(e){}App.currentAudioResolve=null;};const finish=()=>{if(settled)return;settled=true;cleanup();res();};const fail=err=>{if(settled)return;settled=true;cleanup();rej(err||new Error("Audio playback failed"));};App.currentAudioResolve=finish;a.preload="auto";a.playsInline=true;a.playbackRate=PlaybackControls.rate();a.onended=finish;a.onerror=()=>fail(new Error("Audio playback error"));timer=setTimeout(()=>fail(new Error("Audio playback timed out")),45000);a.play().catch(err=>fail(err));});},key(text){return `${$("voiceId").value}|${$("model").value}|${PlaybackControls.rate()}|${text}`;},async eleven(text){let api=$("apiKey").value.trim(),vid=$("voiceId").value.trim();if(!api||!vid){UI.status("Missing ElevenLabs key or Voice ID. Using Alice.","warntxt");return this.system(text);}let key=this.key(text),cached=await Storage.get(AS,key),blob;if(cached?.blob)blob=cached.blob;else{UI.status("Generating ElevenLabs audio…");let r=await fetch(`https://api.elevenlabs.io/v1/text-to-speech/${encodeURIComponent(vid)}`,{method:"POST",headers:{"Accept":"audio/mpeg","Content-Type":"application/json","xi-api-key":api},body:JSON.stringify({text,model_id:$("model").value,voice_settings:{stability:.5,similarity_boost:.75}})});if(!r.ok)throw new Error("ElevenLabs error "+r.status);blob=await r.blob();await Storage.put(AS,{key,blob,createdAt:Date.now()});}return this.playBlob(blob);},async speak(text){if($("voiceMode").value=="eleven"){try{return await this.eleven(text);}catch(e){UI.status("ElevenLabs unavailable for this clip. Using Alice.","warntxt");return this.system(text);}}return this.system(text);}};

class PlaybackEngine{constructor(name,button,statusPrefix=""){this.name=name;this.button=button;this.statusPrefix=statusPrefix;this.run=0;this.playing=false;this.paused=false;this.stopped=false;this.provider=null;}setButton(){if(this.button)this.button.textContent=this.playing?(this.paused?"Resume":"Pause"):"Start";}async wait(run){while(this.paused&&!this.stopped&&run===this.run)await Util.sleep(120);}toggle(providerFactory){if(!this.playing){this.start(providerFactory);return;}if(!this.paused){this.paused=true;speechSynthesis.pause();if(App.currentAudio)App.currentAudio.pause();UI.status((this.statusPrefix||"Playback")+" paused.","warntxt");this.setButton();return;}this.paused=false;speechSynthesis.resume();if(App.currentAudio)App.currentAudio.play().catch(()=>{});UI.status("Playing…");this.setButton();}stop(msg="Stopped."){this.run++;this.stopped=true;this.playing=false;this.paused=false;Speech.stop();App.playbackContext="main";this.setButton();UI.status(msg,"warntxt");}restart(providerFactory,delay=140){this.stop("Restarting…");setTimeout(()=>this.start(providerFactory),delay);}async start(providerFactory){if(this.playing)return;this.run++;let run=this.run;this.playing=true;this.paused=false;this.stopped=false;this.setButton();this.provider=providerFactory();App.playbackContext=this.name;UI.status("Playing…");try{while(run===this.run&&!this.stopped){let item=this.provider.next();if(!item)break;if(item.onBefore)item.onBefore();if(item.label)UI.status(item.label);let reps=item.repeat??1;if(reps==="infinite"){while(run===this.run&&!this.stopped){await this.wait(run);if(run!==this.run||this.stopped)break;await Speech.speak(item.text);await this.wait(run);let pauseMs=PlaybackControls.pause();if(pauseMs>0)await Util.sleep(pauseMs);}}else{for(let i=0;i<Number(reps)&&run===this.run&&!this.stopped;i++){await this.wait(run);if(run!==this.run||this.stopped)break;await Speech.speak(item.text);await this.wait(run);let pauseMs=PlaybackControls.pause();if(pauseMs>0)await Util.sleep(pauseMs);}}}}catch(e){UI.status("Playback error: "+(e&&e.message?e.message:e),"dangertxt");}finally{if(run===this.run){this.playing=false;this.paused=false;this.stopped=false;Speech.stop();App.playbackContext="main";this.setButton();UI.status("Finished.","oktxt");}}}}

const MainPlayer=new PlaybackEngine("main",null,"Sentence playback");
const VerbPlayer=new PlaybackEngine("verb",null,"Verb drill");

const SentenceController={repeat(){let r=PlaybackControls.repeat();return r==="infinite"?1:r;},isLoop(){return PlaybackControls.repeat()==="infinite";},provider(){let scope=$("playMode").value||"group",loop=this.isLoop();if(scope==="current")return this.currentProvider(loop);if(scope==="chapter")return this.sequenceProvider("chapter",loop);return this.sequenceProvider("group",loop);},currentProvider(loop){let done=false;return{next:()=>{let s=Library.current();if(!s)return null;if(done&&!loop)return null;done=true;return{text:s.italian,repeat:this.repeat(),label:(loop?"Looping sentence ":"Sentence ")+s.order,onBefore:()=>UI.renderViewer()};}};},itemsForScope(scope){if(scope==="group")return Library.group();if(scope==="chapter")return Library.chapter();return Library.group();},sequenceProvider(scope,loop){let items=this.itemsForScope(scope),idx=0;if(scope==="group")idx=Math.max(0,Math.min(App.cur.index,items.length-1));else{let cur=Library.current();let pos=items.findIndex(x=>x.id===cur?.id);idx=Math.max(0,pos);}return{next:()=>{if(!items.length)return null;if(idx>=items.length){if(!loop)return null;idx=0;}let s=items[idx++];return{text:s.italian,repeat:this.repeat(),label:(loop?"Looping range — ":"")+"Sentence "+s.order,onBefore:()=>{App.cur.book=s.book;App.cur.chapter=s.chapter;App.cur.group=Util.gnum(s);App.cur.index=Library.group().findIndex(x=>x.id===s.id);if(App.cur.index<0)App.cur.index=0;UI.renderAll();}};}};},toggle(){MainPlayer.toggle(()=>this.provider());},reset(){MainPlayer.stop("Audio engine reset. Press Start to continue.");},restart(){if(MainPlayer.playing)MainPlayer.restart(()=>this.provider());},jumpToIndex(i){App.cur.index=i;UI.renderViewer();if(MainPlayer.playing)MainPlayer.restart(()=>this.provider());},next(){let g=Library.group();if(g.length){App.cur.index=(App.cur.index<g.length-1)?App.cur.index+1:0;}UI.renderViewer();if(MainPlayer.playing)MainPlayer.restart(()=>this.provider());},prev(){let g=Library.group();if(g.length){App.cur.index=(App.cur.index>0)?App.cur.index-1:g.length-1;}UI.renderViewer();if(MainPlayer.playing)MainPlayer.restart(()=>this.provider());}};

const Verb={tenseOrder:["presente","passato","imperfetto","trapassato"],names:{presente:"congiuntivo presente",passato:"congiuntivo passato",imperfetto:"congiuntivo imperfetto",trapassato:"congiuntivo trapassato"},V:{essere:{en:"to be",presente:["sia","sia","sia","siamo","siate","siano"],imperfetto:["fossi","fossi","fosse","fossimo","foste","fossero"],aux:"essere",part:"stato"},avere:{en:"to have",presente:["abbia","abbia","abbia","abbiamo","abbiate","abbiano"],imperfetto:["avessi","avessi","avesse","avessimo","aveste","avessero"],aux:"avere",part:"avuto"},andare:{en:"to go",presente:["vada","vada","vada","andiamo","andiate","vadano"],imperfetto:["andassi","andassi","andasse","andassimo","andaste","andassero"],aux:"essere",part:"andato"},venire:{en:"to come",presente:["venga","venga","venga","veniamo","veniate","vengano"],imperfetto:["venissi","venissi","venisse","venissimo","veniste","venissero"],aux:"essere",part:"venuto"},potere:{en:"to be able to",presente:["possa","possa","possa","possiamo","possiate","possano"],imperfetto:["potessi","potessi","potesse","potessimo","poteste","potessero"],aux:"avere",part:"potuto"},volere:{en:"to want",presente:["voglia","voglia","voglia","vogliamo","vogliate","vogliano"],imperfetto:["volessi","volessi","volesse","volessimo","voleste","volessero"],aux:"avere",part:"voluto"},dovere:{en:"to have to",presente:["debba","debba","debba","dobbiamo","dobbiate","debbano"],imperfetto:["dovessi","dovessi","dovesse","dovessimo","doveste","dovessero"],aux:"avere",part:"dovuto"},dire:{en:"to say",presente:["dica","dica","dica","diciamo","diciate","dicano"],imperfetto:["dicessi","dicessi","dicesse","dicessimo","diceste","dicessero"],aux:"avere",part:"detto"},capire:{en:"to understand",presente:["capisca","capisca","capisca","capiamo","capiate","capiscano"],imperfetto:["capissi","capissi","capisse","capissimo","capiste","capissero"],aux:"avere",part:"capito"},parlare:{en:"to speak",presente:["parli","parli","parli","parliamo","parliate","parlino"],imperfetto:["parlassi","parlassi","parlasse","parlassimo","parlaste","parlassero"],aux:"avere",part:"parlato"},pensare:{en:"to think",presente:["pensi","pensi","pensi","pensiamo","pensiate","pensino"],imperfetto:["pensassi","pensassi","pensasse","pensassimo","pensaste","pensassero"],aux:"avere",part:"pensato"},credere:{en:"to believe",presente:["creda","creda","creda","crediamo","crediate","credano"],imperfetto:["credessi","credessi","credesse","credessimo","credeste","credessero"],aux:"avere",part:"creduto"},sperare:{en:"to hope",presente:["speri","speri","speri","speriamo","speriate","sperino"],imperfetto:["sperassi","sperassi","sperasse","sperassimo","speraste","sperassero"],aux:"avere",part:"sperato"},fare:{en:"to do / make",presente:["faccia","faccia","faccia","facciamo","facciate","facciano"],imperfetto:["facessi","facessi","facesse","facessimo","faceste","facessero"],aux:"avere",part:"fatto"},finire:{en:"to finish",presente:["finisca","finisca","finisca","finiamo","finiate","finiscano"],imperfetto:["finissi","finissi","finisse","finissimo","finiste","finissero"],aux:"avere",part:"finito"}},part(part,i){let p={stato:"stati",andato:"andati",venuto:"venuti"};return i>=3?(p[part]||part):part;},forms(v,t){let d=this.V[v];if(!d)return[];if(t==="presente"||t==="imperfetto")return d[t];let aux=this.V[d.aux][t==="passato"?"presente":"imperfetto"];return aux.map((x,i)=>x+" "+this.part(d.part,i));},line(v,t){return this.forms(v,t).join(", ");},scope(){let s=$("verbScope").value;if(s==="group")return Library.group();if(s==="chapter")return Library.chapter();if(s==="book")return App.sentences.filter(x=>x.book==App.cur.book);return App.sentences;},detect(texts){let all=texts.join(" ").toLowerCase(),found=new Set();Object.keys(this.V).forEach(v=>{[...this.forms(v,"presente"),...this.forms(v,"imperfetto")].forEach(f=>{let simple=f.split(" ")[0];if(new RegExp("\\b"+simple+"\\b","i").test(all))found.add(v);});if(new RegExp("\\b"+v+"\\b","i").test(all))found.add(v);});return[...found].sort(Util.nat);},selectedTense(){return this.tenseOrder[App.verbTenseIndex]||this.tenseOrder[0];},render(){if(!$("verbSel"))return;let old=$("verbSel").value,verbs=this.detect(this.scope().map(s=>s.italian));if(!verbs.length)verbs=Object.keys(this.V).sort(Util.nat);UI.fill($("verbSel"),verbs,verbs.includes(old)?old:verbs[0]);$("detected").innerHTML="Detected/available verbs: "+verbs.map(v=>`<span class="pill">${v}</span>`).join(" ");this.renderView();},renderView(){let v=$("verbSel").value,d=this.V[v];if(!d){$("verbView").innerHTML="";return;}$("verbView").innerHTML=`<div class="verbRef"><strong>${v}</strong> = ${Util.esc(d.en)} · reference: <strong>${Util.esc(this.forms(v,"presente")[0])}</strong></div><div class="tenseGrid">${this.tenseOrder.map(t=>`<div class="tenseCard" id="tense_${t}"><h3>${this.names[t]}</h3><div class="formsLine">${Util.esc(this.line(v,t))}</div></div>`).join("")}</div>`;this.highlight();},highlight(){document.querySelectorAll(".tenseCard").forEach(x=>x.classList.remove("active"));let c=$("tense_"+this.selectedTense());if(c)c.classList.add("active");},provider(){let mode=$("verbMode").value,verbs=[...$("verbSel").options].map(o=>o.value),vi=$("verbSel").selectedIndex<0?0:$("verbSel").selectedIndex,ti=App.verbTenseIndex;if(mode==="once")return this.tenseProvider(verbs,vi,ti,false);if(mode==="looptense")return this.tenseProvider(verbs,vi,ti,true);if(mode==="loopverb")return this.verbProvider(verbs,vi,ti,true,false);if(mode==="nextverb")return this.verbProvider(verbs,vi,ti,false,false);if(mode==="loopverbs")return this.verbProvider(verbs,vi,ti,false,true);return this.tenseProvider(verbs,vi,ti,false);},tenseProvider(verbs,vi,ti,loop){let done=false;return{next:()=>{if(done&&!loop)return null;done=true;let v=verbs[vi],t=this.tenseOrder[ti];return{text:this.line(v,t),repeat:PlaybackControls.repeat(),label:`${v} — ${this.names[t]}`,onBefore:()=>{$("verbSel").selectedIndex=vi;App.verbTenseIndex=ti;this.renderView();}};}};},verbProvider(verbs,vi,ti,loopVerb,loopVerbs){let curV=vi,curT=ti;return{next:()=>{if(curV>=verbs.length){if(loopVerbs)curV=0;else return null;}let v=verbs[curV],t=this.tenseOrder[curT];let item={text:this.line(v,t),repeat:PlaybackControls.repeat(),label:`${v} — ${this.names[t]}`,onBefore:()=>{$("verbSel").selectedIndex=curV;App.verbTenseIndex=curT;this.renderView();}};curT++;if(curT>=this.tenseOrder.length){curT=0;if(!loopVerb)curV++;}return item;}};},toggle(){VerbPlayer.toggle(()=>this.provider());},restart(){if(VerbPlayer.playing)VerbPlayer.restart(()=>this.provider());},moveTense(delta){App.verbTenseIndex+=delta;if(App.verbTenseIndex<0)App.verbTenseIndex=this.tenseOrder.length-1;if(App.verbTenseIndex>=this.tenseOrder.length)App.verbTenseIndex=0;this.renderView();if(VerbPlayer.playing)this.restart();}};


const Editor={sentence:null,open(s){this.sentence=s;$("editItalian").value=s.italian||"";$("editEnglish").value=s.english||"";$("editModal").style.display="flex";setTimeout(()=>$("editItalian").focus(),50);},close(){$("editModal").style.display="none";this.sentence=null;},async save(){let s=this.sentence;if(!s)return;s.italian=$("editItalian").value.trim();s.english=$("editEnglish").value.trim();if(!s.italian){alert("Italian sentence cannot be empty.");return;}await Storage.put(SS,s);this.close();await Library.refresh();UI.status("Sentence updated.","oktxt");}};

const Importer={open(){App.analysed=[];$("importSummary").textContent="No CSV analysed yet.";$("importPreview").innerHTML="";$("importModal").style.display="flex";},preview(items){App.analysed=items;$("importSummary").textContent=items.length?`Detected ${items.length} sentences.`:"No sentences detected.";$("importSummary").className="status "+(items.length?"oktxt":"dangertxt");let sm=items.slice(0,12);$("importPreview").innerHTML=items.length?`<table><thead><tr><th>Book</th><th>Chapter</th><th>#</th><th>Italian</th><th>English</th></tr></thead><tbody>${sm.map(s=>`<tr><td>${Util.esc(s.book)}</td><td>${Util.esc(s.chapter)}</td><td>${s.order}</td><td>${Util.esc(s.italian)}</td><td>${Util.esc(s.english)}</td></tr>`).join("")}</tbody></table>`:"";},async import(){if(!App.analysed.length){alert("Analyse first.");return;}await Storage.addMany(App.analysed);let s=App.analysed[0];App.cur={book:s.book,chapter:s.chapter,group:Util.gnum(s),index:0};App.analysed=[];$("importModal").style.display="none";await Library.refresh();UI.status("Imported successfully.","oktxt");}};

const Nav={nextGroup(render=true){let gs=Util.uniq(Library.chapter().map(Util.gnum)).sort((a,b)=>a-b),i=gs.indexOf(Number(App.cur.group));if(i>=0&&i<gs.length-1){App.cur.group=gs[i+1];App.cur.index=0;if(render)UI.renderAll();return true;}return false;},prevGroup(last=false){let gs=Util.uniq(Library.chapter().map(Util.gnum)).sort((a,b)=>a-b),i=gs.indexOf(Number(App.cur.group));if(i>0){App.cur.group=gs[i-1];App.cur.index=last?Library.group().length-1:0;UI.renderAll();return true;}return false;}};

function toCSV(){let h=["book","chapter","order","italian","english","bookmarked","difficult","notes"];return h.join(",")+"\n"+App.sentences.map(s=>h.map(k=>`"${String(s[k]??"").replace(/"/g,'""')}"`).join(",")).join("\n");}
function download(name,text,type){let b=new Blob([text],{type}),a=document.createElement("a");a.href=URL.createObjectURL(b);a.download=name;a.click();URL.revokeObjectURL(a.href);}

function bind(){MainPlayer.button=$("mainToggle");VerbPlayer.button=$("verbToggle");document.querySelectorAll("[data-panel]").forEach(b=>b.onclick=()=>{document.querySelectorAll("[data-panel]").forEach(x=>x.classList.remove("active"));document.querySelectorAll(".panel").forEach(x=>x.classList.add("hidden"));b.classList.add("active");$(b.dataset.panel).classList.remove("hidden");if(b.dataset.panel==="verbs")Verb.render();});$("openImport").onclick=()=>Importer.open();$("closeImport").onclick=()=>$("importModal").style.display="none";$("analyseFile").onclick=async()=>{let f=$("csvFile").files[0];if(!f){alert("Choose a CSV first.");return;}Importer.preview(Library.parseCSV(await f.text(),{book:$("defaultBook").value,chapter:$("defaultChapter").value}));};$("analysePaste").onclick=()=>Importer.preview(Library.parseCSV($("pasteCsv").value,{book:$("defaultBook").value,chapter:$("defaultChapter").value}));$("importPreviewed").onclick=()=>Importer.import();$("exportCsv").onclick=()=>download("italian-shadowing-library-v101.csv",toCSV(),"text/csv;charset=utf-8");$("clearAll").onclick=async()=>{if(confirm("Delete whole local library and audio cache?")){await Storage.clear(SS);await Storage.clear(AS);App.sentences=[];App.cur={book:"",chapter:"",group:1,index:0};UI.renderAll();}};$("bookSel").onchange=e=>{App.cur.book=e.target.value;App.cur.chapter="";App.cur.group=1;App.cur.index=0;UI.renderAll();};$("chapterSel").onchange=e=>{App.cur.chapter=e.target.value;App.cur.group=1;App.cur.index=0;UI.renderAll();};$("groupSel").onchange=e=>{App.cur.group=Number(e.target.value);App.cur.index=0;UI.renderAll();};$("prevGroup").onclick=()=>Nav.prevGroup();$("nextGroup").onclick=()=>Nav.nextGroup();$("prevSentence").onclick=()=>SentenceController.prev();$("nextSentence").onclick=()=>SentenceController.next();$("mainToggle").onclick=()=>SentenceController.toggle();$("hardReset").onclick=()=>{MainPlayer.stop("Audio reset.");VerbPlayer.stop("Audio reset.");};["displayMode","showEnglish"].forEach(id=>$(id).onchange=()=>UI.renderViewer());$("playMode").onchange=()=>SentenceController.restart();$("search").oninput=()=>UI.renderViewer();$("closeEdit").onclick=()=>Editor.close();$("closeEditBottom").onclick=()=>Editor.close();$("saveEdit").onclick=()=>Editor.save();$("editModal").onclick=e=>{if(e.target===$("editModal"))Editor.close();};["repeat","rate","pause","verbRepeat","verbRate","verbPause"].forEach(id=>{if($(id))$(id).onchange=()=>{if(MainPlayer.playing)SentenceController.restart();if(VerbPlayer.playing)Verb.restart();};});$("voiceMode").onchange=()=>{localStorage.setItem("v08voiceMode",$("voiceMode").value);$("elevenPanel").classList.toggle("hidden",$("voiceMode").value!=="eleven");};$("saveElevenBtn").onclick=()=>{if($("saveEleven").value==="yes"){localStorage.setItem("v08key",$("apiKey").value);localStorage.setItem("v08voice",$("voiceId").value);localStorage.setItem("v08model",$("model").value);localStorage.setItem("v08voiceMode","eleven");$("voiceMode").value="eleven";$("elevenPanel").classList.remove("hidden");UI.status("ElevenLabs settings saved.","oktxt");}};$("clearElevenBtn").onclick=()=>{["v08key","v08voice","v08model"].forEach(k=>localStorage.removeItem(k));$("apiKey").value="";$("voiceId").value="";UI.status("ElevenLabs settings cleared.","warntxt");};$("verbScope").onchange=()=>Verb.render();$("verbSel").onchange=()=>{App.verbTenseIndex=0;Verb.renderView();Verb.restart();};$("verbMode").onchange=()=>Verb.restart();$("verbToggle").onclick=()=>Verb.toggle();$("prevTense").onclick=()=>Verb.moveTense(-1);$("nextTense").onclick=()=>Verb.moveTense(1);$("showBookmarks").onclick=()=>{$("reviewView").innerHTML=App.sentences.filter(s=>s.bookmarked).map(s=>`<div class="card"><div class="italian">${Util.esc(s.italian)}</div><div class="english">${Util.esc(s.english)}</div></div>`).join("")||"<p>No bookmarked sentences.</p>";};$("showAll").onclick=()=>{$("reviewView").innerHTML=App.sentences.map(s=>`<div class="card"><span class="pill">${Util.esc(s.book)} / ${Util.esc(s.chapter)} / ${s.order}</span><div class="italian">${Util.esc(s.italian)}</div><div class="english">${Util.esc(s.english)}</div></div>`).join("");};}

window.speechSynthesis.onvoiceschanged=()=>Speech.loadVoices();
(async function init(){App.db=await Storage.open();bind();Speech.loadVoices();$("apiKey").value=localStorage.getItem("v08key")||"";$("voiceId").value=localStorage.getItem("v08voice")||"";$("model").value=localStorage.getItem("v08model")||"eleven_multilingual_v2";$("voiceMode").value=localStorage.getItem("v08voiceMode")||"eleven";$("elevenPanel").classList.toggle("hidden",$("voiceMode").value!=="eleven");await Library.refresh();MainPlayer.setButton();VerbPlayer.setButton();})();
